# 版本日志

本文件记录已经发布的版本。版本号遵循语义化版本规则。

## 未发布

### 调整

- MinerU Markdown 不可用时，将“重新检查”按钮移到红色提示标题旁；解析完成后可直接刷新当前附件状态，无需重启 Zotero。

## 1.4.3 - 2026-08-12

### 调整

- MinerU Markdown 缺失或缓存不完整时，侧栏会明确说明文本划词和手动输入仍可翻译，同时提示图片取词与知识准备暂不可用。

### 修复

- 解除文本翻译对 MinerU Markdown 的硬依赖。有非空划词或手动输入时会使用当前选定模型直接翻译；只有经过校验的论文上下文存在时才注入 Markdown、同步论文状态并启动知识准备。
- 保持上下文安全边界不变：图片取词继续要求有效的 MinerU 来源与索引，已有缓存的身份不匹配、内容损坏等校验错误仍会显式失败，不会被无上下文翻译隐藏。

## 1.4.2 - 2026-08-11

### 调整

- 同一论文段落附近的划词翻译现在复用稳定的连续上下文窗口，减少相邻选区因提示词前缀变化造成的缓存失效和响应波动。

### 修复

- 修复多行翻译结果生成 HTML 换行标签后，在 Zotero XHTML 界面触发 `SyntaxError: An invalid or illegal string was specified`、只显示部分译文的问题。
- 修复跨页划词时，第二页顶部的 Fig./Figure、Algorithm 或表格内容混入后续正文翻译的问题。过滤过程只在 Zotero 运行时选区文本、页码和矩形全部一致时生效；正文中的图、算法引用仍会保留。

## 1.4.1 - 2026-07-30

### 修复

- 修复在 Reader 划词翻译浮窗中编辑原文时，`Delete` 和 `Backspace` 被 Zotero 的全局快捷键捕获并关闭浮窗的问题。现在这两个按键只删除输入框中的文字，浮窗会保持打开。

## 1.4.0 - 2026-07-27

### 新增

- 设置页支持创建多个模型服务商。每个服务商可选择 `Codex Auth` 或 `OpenAI Compatible`，并保存多个模型 ID；OpenAI-compatible 接口统一使用 HTTPS `/chat/completions` 和显式 API Key。
- 设置页与 Reader 侧栏新增共用的当前模型选择。翻译、论文背景、术语、外部补充和图片 OCR 始终只走用户显式选择的同一个服务商与模型。
- 每个模型保留独立的连接测试按钮。测试结果只提供配置反馈，不限制保存或选择。

### 调整

- Codex 登录文件缺失、损坏，refresh token 缺失、失效或已使用，以及刷新凭据无法写回时，模型测试、划词翻译和图片取词现在统一提示用户运行 `codex login`。刷新流程内部出现的 `SyntaxError` 不再直接暴露给用户；非认证流程中的同名错误仍按原样报告，避免把 DOM、路径或接口缺陷误判为登录过期。
- 修复 Codex CLI 或其他插件与本插件同时轮换单次 refresh token 时，本插件直接暴露 `refresh_token_reused` 的竞态。刷新被认证服务器拒绝后，本插件会重新读取同一个 `auth.json`：仅在发现外部已写入不同的新 access token 时继续原请求；文件未变化时停止并明确提示执行 `codex login`，不会重复使用旧 refresh token。
- 修复 `Codex Auth` 在插件模块无法取得 XPCOM `nsIFile` 接口时，解析 `auth.json` 路径会抛出 `SyntaxError: An invalid or illegal string was specified` 的问题。认证路径现在按 `llm-for-zotero` 的顺序优先读取环境变量和 Zotero `PathUtils.homeDir`，仅在接口明确可用时调用目录服务。
- 修复部分 Reader 实例 ID 包含 CSS 保留字符时，Codex Auth 流式译文刷新会触发 `SyntaxError: An invalid or illegal string was specified` 的问题。浮窗控件改为按精确 DOM ID 查找，不再把动态实例 ID 解析为 CSS 选择器。
- OpenAI-compatible 服务商明确拒绝 `image_url`、多模态或视觉输入时，图片取词改为显示“当前模型不支持图片输入”，不再向用户暴露底层 JSON 反序列化错误。认证、额度和其他请求错误仍保留原始原因。
- 服务商卡片改为紧凑布局，默认按“服务商 A、服务商 B……”命名且可直接重命名；删除按钮统一为 `×`，模型的测试按钮在窄设置窗口中保持可见，保存按钮精简为“保存”。
- 修复动态设置文案加载了错误的本地化资源，导致界面显示 `papertranslateforzotero-pref-...` 完整键名的问题。
- 原有 Codex 设置会确定性迁移为“服务商 A”和当前模型，不影响已有用户继续使用 `auth.json`；已经自定义的其他服务商名称保持不变。
- OpenAI-compatible 请求只发送模型、消息、流式开关及任务实际需要的图片输入，不擅自添加推理强度、temperature 或 token 参数。
- 通用 Chat Completions 协议不提供统一网页搜索；选择 OpenAI-compatible 模型时，外部补充会明确显示协议不支持，且不会回退到 Codex。论文内知识、翻译和模型可接受图片时的 OCR 不受影响。
- 切换当前模型或修改当前模型配置时会取消正在运行的模型任务；已经校验并持久化的知识文件不会删除。

### 安全

- OpenAI-compatible API Key 只保存在 Zotero 插件偏好中，不写入论文上下文、日志、版本库或导出文件。

## 1.3.0 - 2026-07-27

### 新增

- Reader 浮层与侧栏译文区支持安全的 Markdown 和 LaTeX 数学渲染。`$c_1$`、`$x^2$` 等表达式会使用 MathML 显示，不再暴露美元符号和下划线源码；段落、列表、强调和代码格式共用同一渲染实现。

### 安全

- Markdown 禁止原始 HTML、链接和图片，LaTeX 使用不受信任模式并限制宏展开与尺寸。无法解析的闭合公式会以红色点线明确标记，不执行或静默替换模型内容。

## 1.2.2 - 2026-07-27

### 修复

- 外部补充返回无法与本次 Responses URL citation 对应的来源时，不再把整个知识阶段标记为警告。该轮摘要和来源会被丢弃，并按零个可采纳来源正常完成；请求失败、协议损坏、JSON 解析错误和文件写入失败仍会明确显示警告。

## 1.2.1 - 2026-07-26

### 修复

- 修复 Zotero PDF.js 只渲染当前页面附近内容时，图片取词可能误判页面缺少 Canvas 的问题。插件现在先通过页面容器确定选中页，再读取该页的完整渲染区域，不再要求整篇论文的页面全部完成渲染。
- 图表中的标签会按语义重新排版。同一标签、短语或句子中因框体宽度产生的折行会合并，独立标签、列表、表格、段落、图注和公式仍保留原有边界。旧版 OCR 缓存不会继续复用。
- 修复图片取词遇到网络错误后按钮一直不可用的问题。错误仍会显示，但当前任务会先正确结束，用户可以立即再次点击“图片取词”。

## 1.2.0 - 2026-07-24

### 图片区域 OCR

- 新增手动图片区域识别：在 Reader 中框选论文图片里的文字后，插件通过页码、选区坐标和 `content_list.json` 定位唯一的 MinerU 图片资产，再把对应裁剪区域交给当前 Codex 模型逐字识别。
- OCR 结果先进入可编辑原文框，确认后沿用现有论文上下文翻译；识别阶段要求保留段落、公式、符号、缩写、数值和单位，不生成翻译或解释。
- 新增按附件、图片 SHA-256、裁剪坐标和模型区分的识别缓存，避免同一区域重复请求。OCR 结果不进入章节索引、论文背景或术语表。
- 图片映射缺失、多个图片块同时命中、路径越界、非受支持图片或模型不接受图片时会明确报错。插件不截取 PDF 像素、不读取 PDF 文件、不调用 MinerU，也不切换 OCR 引擎、接口或模型。

### 外部知识收口

- 取消外部来源数量要求。已有资料足以消除翻译歧义时立即结束；成功请求得到零个或一个可采纳来源都属于正常完成。
- 外部补充最多提出 3 个论文内生问题，计划不超过两次网页搜索，第三次只作为流解析的硬性安全上限；不会为了凑满来源继续扩展查询。
- 返回 403、429、robots 限制、无法连接或没有可核验正文的候选页面会直接过滤，不再写入 `background-sources.json`，侧栏也不再显示“来源受限”。
- 只有整个 Codex 请求、响应协议、结果解析或配对文件写入失败时才标记外部阶段警告。旧版空结果和来源受限状态会按新语义确定性恢复，不会触发自动重试。

### 工程与文档

- 补充 MinerU 图片路径边界、选区映射、OCR 请求与缓存验证，并覆盖外部空结果、单来源收口、搜索调用上限和旧状态迁移。
- 更新安装与使用说明，明确 `full.md` 仍是唯一论文语义上下文，以及图片 OCR 对 `content_list.json` 和本地 MinerU 图片的要求。
- 将 `llm-for-zotero` 参考版本更新到 v3.8.30，并记录 Responses 图片输入、Reader 页定位和选区浮层的具体复用边界。

## 1.1.0 - 2026-07-23

### 知识准备

- 将论文知识准备收敛为固定顺序的一次性流程：论文背景完成后再写术语，最后仅进行一轮可选外部补充。
- 论文内提取均衡读取最多 8,000 个字符，生成五项非空背景字段和 6–12 个正文可逐字定位的高价值术语后立即结束；核心与外部流分别限制为 16,000/8,000 个可见字符和 2,000,000/1,000,000 个响应字节，外部补充最多 3 个问题、3 次搜索和 3 个来源，不递归扩展或自动重试。
- 修复同一论文的并发文件写入和阶段所有权冲突。`complete / warning / error / skipped` 均为终态，侧栏进度不会再从 3/5 退回 2/5。
- 模型请求长时间不返回时会被取消并显式标记失败或警告；卡死检测不作为成功条件，且不影响已经开放的 Markdown 翻译。
- `_preparation.json`、背景、术语和来源统一走每篇论文的串行原子写入；索引新增 manifest 哈希，来源变化时确定性失效。
- 重启恢复会重新解析五项背景字段和完整术语行，并逐项核对正文原词、来源等级、置信度和时间；不再用标题标记或表格行数冒充完成。
- 修复旧 Markdown 请求晚返回后覆盖新版背景、来源或术语的竞态；每次写入都会在目录锁内重新核对当前来源哈希和准备记录。
- 索引恢复改为与当前 Markdown 重新生成的分块逐项比对，空分块、越界偏移和错误邻接会直接重建，不再被标为完成。
- 修复知识准备向旧版 ChatGPT Codex 后端发送不兼容的 `max_output_tokens` 所导致的 HTTP 400；同时按 `llm-for-zotero` 的真实旧版协议移除 `max_tool_calls`，不做失败后的换参数重试。
- 输出量、完整流大小和网页搜索次数改由本地流解析器硬性限制，越界即取消并显式报错；外部检索只保存能与 Responses URL citation 精确对应的 HTTPS 来源，无来源的模型背景不会进入翻译上下文。
- 术语只在一次核心分析中生成 6–12 项，不再在每次翻译后发起额外术语请求；重启时发现背景、术语或外部来源只写了一半，会把未完成阶段明确收口，不再重新执行整篇论文分析。
- 修复划词翻译重新读取上下文时误停当前后台任务的问题；只有任务调度器能把重启后遗留的 `running` 判为陈旧，普通刷新不会改写活跃阶段。
- 可选知识文件损坏不再阻断来源与索引已就绪的翻译。完成记录保留单调终态，独立完整性问题写入 `_preparation.json`，无效内容不会注入模型。
- 外部摘要与来源记录增加同次内容哈希，并采用失败回滚的双文件写入；不会留下无来源摘要或错误的完成状态。
- `_preparation.json` 新增独立的准备 schema、当前 attempt 和只追加的历史快照。普通重开不再重新执行失败任务；人工重试会创建新 attempt，并保留旧终态供排查。
- 新 attempt 会在每次背景、术语、来源和阶段写入前核对论文身份、Markdown 哈希及 attempt ID，拒绝旧请求晚返回后覆盖新结果。
- 核心重试从第一个失败阶段继续，保留已校验背景和人工术语；外部重试只重置配对的外部摘要与来源记录，任一写入失败都会回滚。
- 缺少全部 MinerU 缓存文件与“缓存只缺一部分”现在使用不同错误类型。侧栏不再展示绝对路径，也不会把损坏缓存误判成未配置 Token。

### Reader 与界面

- 侧栏新增协调样式的“打开目录”按钮，只打开当前论文经过身份校验的知识目录。
- Reader 浮层改为一个可缩放容器；拖动右下角时，原文与译文区域按同一比例一起变化，流式输出不会自行改尺寸。
- 修复相同文本在不同附件间串用任务、空白选区沿用旧任务，以及侧栏重绘重复启动知识准备的问题。
- 修复快速切换 Reader 条目时上下文未缓存、侧栏编辑后仍显示旧译文，以及无任务浮层误显示“正在翻译”的问题。
- 流式翻译期间编辑原文会取消旧请求并创建独立任务，旧译文不会写回新原文。
- 将译文流刷新与知识文件刷新拆开，并按附件、Markdown 哈希和刷新序号拒绝晚到的旧读取；连续选区时，旧任务结束事件也不会再截断新任务的流式显示。
- Reader 关闭或切离附件时只取消该附件的活动翻译；知识文件行名和状态提示补齐中英文界面文本。
- 保留公式中的上标字符、独立数字正文和有语义的连字符；只有经 MinerU Markdown 逐字证明时才合并跨行断词。
- 文件列表旁新增“停止准备 / 重新准备 / 重试外部补充”动态按钮。停止只取消当前论文的知识任务，来源与索引完成后的翻译保持可用。
- MinerU Markdown 尚未生成时，在文件列表下方显示紧凑说明、Token 管理入口和“重新检查”按钮，不增加单独帮助卡。

### 稳定性与设置

- 目标语言固定为中文，避免同一论文术语文件混入不同目标语言；设置页显示只读值。
- 补齐 Reader、侧栏、通知和设置面板的显式注销，启动失败时回滚已注册组件。
- 插件关闭时取消仍在运行的核心背景与外部补充请求；翻译完成后不再启动额外术语请求。
- Codex 登录文件刷新改为串行原子更新，避免与 Codex CLI 或 `llm-for-zotero` 同时刷新时覆盖新令牌。
- Codex 共享令牌刷新增加独立 30 秒上限和关闭取消；SSE 解析支持跨网络分块的 CRLF，并校验网页引用事件。
- 设置页连通性测试增加 30 秒取消和关闭清理；OAuth 刷新期间若 Codex CLI 已写入新令牌，本插件保留并使用 CLI 的新值，不会覆盖登录状态。
- 修复永久更新地址的 `update.json` 发布流程，并增加状态机、并发写入、选区清理、任务隔离和联动缩放测试。
- CI 和发布流程新增整套测试 60 秒上限、完整 Prettier 范围、XPI 内 manifest/私有内容及 `update.json` 校验；同版本同内容资产会跳过，不同内容会拒绝替换，固定更新清单也拒绝版本倒退。
- 新增 MinerU 缓存分类、旧状态迁移、attempt 归档、晚写屏障、外部配对重置、跨 Markdown 版本停止隔离、重试交接和阶段间隙停止测试；本版本共通过 98 项自动化测试。

## 1.0.0 - 2026-07-17

这是 `paper-translate-for-zotero` 的第一个正式版本。

### 翻译流程

- 从 `llm-for-zotero` 已生成并通过来源校验的 MinerU `full.md` 获取论文上下文，不读取 PDF，也不调用 MinerU。
- 论文来源和章节索引写完后立即开放翻译。背景分析、术语提取和网页检索转入后台，不再占用当前翻译的等待时间。
- 每次翻译只提交选中文本、相关正文片段、已积累的背景和术语，不重复发送全文。
- 新选区会取消当前翻译并替换原文；流式输出只显示译文。

### 选区与排版

- 过滤跨页选区中的 IEEE 版权号、授权提示、下载记录、限制提示和独立页码。
- 合并 PDF 正文的视觉断行，保留空段、项目符号和列表顺序。
- 要求模型按原文的段落和列表换行；流式结果会再次整理项目符号边界。
- Reader 浮层和侧栏中的原文框、译文框采用稳定的初始尺寸，输出过程中不再反复缩放。

### 论文知识文件

- 每篇论文使用 Zotero 父条目 key 建立独立目录。
- 新增 `_paper_source.json`、`_preparation.json`、`index.json`、`background.md`、`terminology.md` 和 `background-sources.json`。
- MinerU manifest 没有章节时，从 Markdown 的 `#` 到 `####` 标题重建章节和 UTF-16 偏移。
- 背景内容区分论文依据与外部补充。双语术语表记录原文写法、规范英文、中文译法、正文证据、来源等级和置信度。
- Markdown 哈希变化后重建索引并重新核对术语。正文仍有对应原词时，保留人工修改过的译法。
- 文件内容和进度状态使用原子写入；并发术语更新按论文串行落盘，避免互相覆盖。

### 背景检索

- 不再把 Crossref、Semantic Scholar 或任何固定网站设为翻译前置条件。
- 先根据论文内容生成检索问题，再由 Codex 进行可选网页搜索。
- 论文正文以及官方、标准资料优先；学术来源用于补充，社区页面只解释通用概念。
- 网络受限或没有结果时记录 `warning`，不阻断基于 Markdown 的翻译。

### 界面与设置

- Reader 侧栏显示论文标题、作者、年份、`MD` 标记和实际文件进度，不再输出整段背景或完整错误网址。
- 设置页名称固定为 `paper-translate-for-zotero`。
- 认证模式显示为不可编辑的 `Codex Auth`，删除界面中的 `(Legacy)` 字样和说明小字。
- 默认模型为 `gpt-5.4`，默认推理强度为 `medium`。
- 保留模型连通性测试。

### 独立性与错误处理

- 插件使用自己的 addon ID、偏好、DOM、CSS、提示词、任务和上下文目录，不读取或修改 `llm-for-zotero` 对话。
- Codex 请求使用 CLI 登录凭据和固定 Responses 地址，不启动 App Server，也不切换协议、模型或服务商。
- 来源冲突、缓存损坏、流式响应错误和路径越界会直接报错，不添加 PDF 回退或伪造成功结果。
- Zotero 条目进入回收站时保留上下文；只有父条目永久删除后才按路径和身份检查结果清理目录。

### 工程与发布

- 添加 AGPL-3.0-or-later 许可、NOTICE、上游复用说明、GitHub Actions CI 和标签发布流程。
- 发布前通过 36 项单元测试、TypeScript、ESLint、Prettier、生产构建和 XPI 内容检查。
